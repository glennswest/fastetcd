//! Generated etcd v3 gRPC stubs.
//!
//! The `.proto` files live under `crates/proto/protos/` and are vendored from
//! `etcd-io/etcd/api/`. The `build.rs` runs `tonic-build` to emit stubs at
//! compile time. Re-exports are added as services are wired up.

// Stubs will be wired in once protos are vendored. Keeping the lib empty
// for now so the workspace builds.
